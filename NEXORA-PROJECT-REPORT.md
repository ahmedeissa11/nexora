# تقرير مشروع NEXORA
## البيت الإلكتروني الكامل — من الهوية البصرية حتى حجوزات المخزون

**العلامة:** NEXORA  
**الوصف:** Technology, curated.  
**المقر المتخيّل:** Lisbon · Est. 2019  
**تاريخ التقرير:** 7 سبتمبر 2026  
**الحالة الحالية:** نهاية المرحلة 6 — المتجر يعمل، الدفع الحقيقي (Stripe) والإدارة (Admin) لم يُنفَّذا عمداً.

---

## 0. خلاصة تنفيذية

NEXORA متجر بوتيك لتقنية فاخرة: واجهة سينمائية داكنة، كتالوج حقيقي، سلة، حساب، وطلب. الواجهة **Vanilla JS SPA** بلا React/Next/Vue وبلا TypeScript على الفرونت وبلا Bundler. الخلفية **Express + Prisma 5 + PostgreSQL**.

ما اكتمل حتى الآن:

| الطبقة | الحالة |
|---|---|
| الهوية البصرية والواجهة | مكتملة ومقفلة (`styles.css?v=15`) |
| الكتالوج، البحث، الفلاتر، PDP، المقارنة | مكتمل |
| السلة، المفضلة، الحساب، الداشبورد | مكتمل وظيفياً |
| API + Postgres + جلسات HttpOnly | مكتمل |
| التأمين (المرحلة 5) | مكتمل ومُختبَر |
| حجوزات المخزون ودورة الطلب (المرحلة 6) | مكتمل ومُختبَر |
| Stripe / دفع حقيقي | **لم يبدأ — المرحلة 7** |
| لوحة Admin | **لم تبدأ** |

**الجملة المهمة:** الضغط على «Place order» لم يعد يأكل المخزون للأبد. ينشئ طلباً `pending_payment` وحجزاً لمدة 15 دقيقة. المخزون الفيزيائي لا يُخصم إلا بعد تأكيد دفع داخلي جاهز لـ Stripe لاحقاً.

---

## 1. ما هو NEXORA؟

متجر إلكتروني بأسلوب دار صغيرة، لا سوبرماركت تقني. اللغة البصرية:

- داكن فاخر، حواف دائرية، حدود رفيعة، زجاج خفيف، سينمائي.
- منتجات مضاءة في استوديو أسود: تقريباً 90٪ أسود/فحمي، 7٪ أزرق/نيلي/بنفسجي على الحافة، 3٪ سماوي. إضاءة حافة وانعكاس فقط — ليست نيون وليست سايبربانك.
- نيون الواجهة في أضيق الحدود: لا حدود سماوية ساطعة، لا تدرجات بنفسجية قوية، لا توهج على كل شيء.
- التباين المقصود: **واجهة داكنة هادئة + منتجات مضيئة**.

العلامة كانت في مرحلة سابقة باسم AETHER ثم أُعيدت إلى **NEXORA** بناءً على توجيه صريح.

القطع الأصلية الثمانية بقيت بياناتها كما هي:

`One` · `Drift` · `Field` · `Halo` · `Meridian` · `Type` · `Vessel` · `Arc`

ثم توسّع الكتالوج إلى نحو **64 قطعة** في قاعدة البيانات (السيد يغذيها)، مع فئات البوتيك السبع:

Headphones · Smart Watches · Keyboards · Mice · Speakers · Sneakers · Accessories

كل فئة لها صور استوديو خاصة، ليست صور `_studio.jpg` الاحتياطية وليست صوراً مكررة بين SKUs.

---

## 2. قرارات المنتج التي ثبتت (ولا تُكسر)

هذه ليست تفضيلات عابرة؛ هي قيود تنفيذ:

1. **لا إعادة تصميم كاملة.** تطوير الهوية الموجودة، لا لغة تصميم جديدة.
2. **الواجهة مقفلة بعد الترقية.** لا تعديل `styles.css` ولا التخطيط ولا التايبوغرافي ولا الـ overlays إلا للحد الأدنى من JS.
3. **فرونت Vanilla فقط.** لا React/Next/Vue، لا TypeScript على الواجهة، لا Webpack/Vite.
4. **شاشة الحساب واحدة:** Email + Password + Continue. إن وُجد الإيميل يُسجَّل الدخول، وإلا يُنشأ حساب.
5. **لا توكن في `localStorage`.** الجلسة HttpOnly cookie فقط (`nexora_sid`).
6. **`nexora-cart` كاش محلي فقط.** قاعدة البيانات هي المرجع. بعد الدخول تُدمج سلة الضيف. بعد الطلب الناجح تُفرَّغ السلة محلياً وفي DB. بعد الخروج لا تُعاد سلة المستخدم السابق إلى ضيف جديد.
7. **المرحلة 4 فما بعد:** بلا Stripe/PayPal، بلا Admin statuses، بلا مزامنة wishlist، بلا مراجعات مستخدمين، بلا إيميلات.
8. **المرحلة 5:** إصلاحات أمنية فقط. لا تغيير معمارية المخزون وقتها، ولا Stripe.
9. **المرحلة 6:** حجوزات + state machine. لا Stripe، لا Admin، لا إعادة تصميم checkout.

---

## 3. معمارية النظام

```
المتصفح
  index.html          SPA واحد، hash routes
  styles.css?v=15     مقفول
  catalog.js?v=3      كتالوج احتياطي إن سقط الـ API
  api.js?v=2          fetch same-origin + credentials
  app.js?v=18         الحالة، الرسم، السلة، الحساب، الطلب
        │
        │  /api/*   JSON + cookies
        ▼
Express 0.0.0.0:3000
  Helmet CSP / cookies / rate limits / origin guard
  Prisma 5  ──►  PostgreSQL (nexora)
```

**توجيه الصفحات (hash):**

| المسار | الصفحة |
|---|---|
| `#/` | الرئيسية |
| `#/shop` | المتجر (بحث + فئة + سعر + ترتيب + صفحات) |
| `#/collections` | الفئات |
| `#/arrivals` | الوصولات |
| `#/deals` | العروض |
| `#/atelier` | عن الدار |
| `#/journal` | اليوميات |
| `#/product/:id` | صفحة المنتج |
| `#/compare` | المقارنة (حتى 4 قطع) |
| `#/dashboard` | حساب المستخدم وطلباته |

Overlays (بدون تغيير مسار): بحث، سلة، مفضلة، حساب، checkout، quick view، مقال، قائمة موبايل.

---

## 4. الواجهة — ماذا بُني

### 4.1 الهيكل
- شريط علوي: الشعار الدائري، تنقّل، بحث، مفضلة، حساب، سلة.
- بطل رئيسي + شريط ثقة (خامات، انتقاء، دفع آمن، توصيل).
- The Nexora Drop مع عدّاد.
- شبكات منتجات، فئات عريضة، افتتاحيات editorial، يوميات، تذييل Lisbon.

### 4.2 المتجر
- 7 شرائح فئات فقط (رُفض صف الشرائح المتراكب السابق).
- بحث، فلتر سعر، ترتيب، ترقيم صفحات 12/صفحة.
- بطاقة: صورة، فئة، اسم، تقييم، مخزون بصري، سعر، Add / Quick view / Compare.
- PDP كاملة: مواصفات، ملاحظات الدار، قطع ذات صلة، إضافات للسلة.

### 4.3 التجارة على الواجهة
- سلة جانبية، إنهاء طلب بحقول: الاسم، اللقب، الإيميل، العنوان.
- نص الدفع الحالي صادق نسبياً: «Payment is simulated in this preview.» — Stripe غير موجود بعد.
- بعد `POST /api/orders` تظهر لوحة النجاح مع رقم الطلب `NX-xxxx`.
- المفضلة محلية (`nexora-wish`). ليست متزامنة مع السيرفر عمداً.

### 4.4 الحساب
- Continue = login أو register.
- الداشبورد يعرض الإيميل وقائمة الطلبات وحالاتها الجديدة (`pending_payment` وغيرها).

**ما لم يُمس بعد قفل الواجهة:** `styles.css`، تخطيط الـ checkout، درج السلة، تصميم الحساب، الأنيميشن، الـ hash routes.

---

## 5. الكتالوج والصور

- سيد قاعدة البيانات: **64 منتجاً**، مقالتان، Drop بتاريخ لاحق.
- `catalog.js` احتياطي للمتصفح إن تعذّر `/api/products`.
- صور الاستوديو في `images/` (حوالي 39 ملف JPG + `_studio.jpg` احتياطي).
- سياسة الصور: لا إعادة استخدام صورة بين SKUs، لا خلفية بيضاء، إضاءة الاستوديو الداكن تبقى ظاهرة.
- ملاحظة معروفة: بعض الـ SKUs ما زالت بلا ملف JPG مخصص كامل؛ الواجهة تسقط بهدوء إلى `_studio.jpg` عبر مستمع `error` (بدون `onerror=` إنلاين حتى لا يكسر CSP).

الفئات وارتباطها في الـ API:

| شريحة المتجر | فئات قاعدة البيانات |
|---|---|
| Headphones | Headphones, Audio |
| Smart Watches | Smart Watches, Time |
| Keyboards | Keyboards, Desk |
| Mice | Mice |
| Speakers | Speakers |
| Sneakers | Sneakers |
| Accessories | Accessories, Light |

ثلاث تشطيبات مشتركة: **graphite / midnight / oxide**.

---

## 6. الخلفية — الملفات والخدمات

```
server/
  prisma/schema.prisma
  prisma/migrations/          5 ترحيلات
  prisma/seed.js
  src/index.js                Helmet + static SPA + sweeper
  src/config/index.js
  src/db/prisma.js
  src/middleware/
    auth.js                   attachUser من cookie الجلسة
    security.js               rate limits, origin, no-store, URI
    error.js                  JSON 404 / Invalid JSON / 500 آمن
  src/routes/                 health, products, search, home,
                              articles, cart, auth, orders, /me
  src/services/
    authService.js
    sessionService.js
    cartService.js
    productService.js
    homeService.js
    articleService.js
    orderService.js
    inventoryService.js       حجوزات + انتهاء المهلة
```

**التقنيات:** Node 20 · Express 4 · Prisma 5.22 · PostgreSQL 17 · bcrypt · Helmet 8 · express-rate-limit · cookie-parser.

**متغيرات البيئة:**

```
DATABASE_URL
SESSION_SECRET
PORT=3000
NODE_ENV=development
ORDER_RESERVATION_MINUTES=15
```

---

## 7. واجهة الـ API

كلها JSON، نفس الأصل، الكوكيز تُرسل تلقائياً.

| الطريقة | المسار | ماذا يفعل |
|---|---|---|
| GET | `/api/health` | `{ status: "ok" }` (+ `database` في التطوير) |
| GET | `/api/products` | قائمة مع cat/price/sort/q/page/perPage |
| GET | `/api/products/:id` | PDP + مواصفات + related |
| GET | `/api/search?q=` | بحث حتى 24 نتيجة |
| GET | `/api/home` | featured / drop / arrivals / deals |
| GET | `/api/articles` | اليوميات |
| GET | `/api/cart` | السلة الرسمية |
| POST | `/api/cart` | إضافة سطر |
| PATCH | `/api/cart` | تعديل الكمية |
| DELETE | `/api/cart/:id/:finish` | حذف سطر |
| POST | `/api/auth/register` | إنشاء + دمج سلة الضيف |
| POST | `/api/auth/login` | دخول + دمج سلة الضيف |
| POST | `/api/auth/logout` | إنهاء الجلسة |
| GET | `/api/me` | المستخدم الحالي |
| POST | `/api/orders` | إنشاء `pending_payment` + حجز |
| GET | `/api/orders` | طلبات المستخدم (يتطلب دخول) |
| GET | `/api/orders/:id` | طلب واحد معزول بالمالك |

لا يوجد مسار عام لتأكيد الدفع. `confirmPaidOrder` خدمة داخلية فقط — مخصّصة لـ webhook Stripe في المرحلة 7.

---

## 8. الحساب والجلسات

- كلمة المرور: 8 أحرف على الأقل، bcrypt 12 جولة.
- الرسالة موحّدة: `Invalid email or password` للدخول الفاشل وللتسجيل على إيميل موجود (ضد تعداد الحسابات).
- الدخول دائماً يعمل `bcrypt.compare` حتى لو الإيميل غير موجود (hash وهمي) حتى لا يتسرّب التوقيت.
- الجلسة: 32 بايت عشوائي في الكوكي `nexora_sid`، وفي قاعدة البيانات HMAC-SHA256.
- الكوكي: `HttpOnly` · `SameSite=Lax` · `Path=/` · `Secure` في الإنتاج فقط · 14 يوماً.
- لا CORS بنجمة. الطلبات المعدِّلة تُفحص `Origin` إن وُجد؛ غياب Origin مسموح (curl والـ SPA نفسه).

**دمج السلة عند الدخول:** سلة الضيف (`cart_token`) تُدمج في سلة المستخدم ثم يُمسح كوكي الضيف. المخزون الناقص يُسقط من الدمج بهدوء.

**بعد الخروج:** جلسة تُحذف. ضيف جديد يحصل على سلة فارغة. الواجهة تخزّن `nexora-cart` بالشكل `{ owner, items }` حتى لا تُعاد سلة المستخدم السابق إلى ضيف.

---

## 9. السلة

- المرجع: جدول `Cart` / `CartItem`.
- الضيف: كوكي `cart_token` (خام في المتصفح، **مشفّر HMAC بالبادئة `v1:` في قاعدة البيانات**). الصفوف القديمة ذات النص الصريح تُرحَّل عند القراءة.
- المستخدم: سلة مربوطة بـ `userId`، بدون token.
- الأسعار تُحسب في السيرفر من جدول المنتج. أي `price` يرسله العميل يُتجاهل.
- الكمية عدد صحيح 1–99. `qty: null` أو نص أو `finish` كمصفوفة/كائن → 400.
- التشطيب إن غاب → `graphite`.
- حد السلة لا يتجاوز **المتاح** (المخزون الفيزيائي − الحجوزات النشطة)، لا المخزون الفيزيائي وحده.

`nexora-cart` في المتصفح كاش فقط. بعد طلب ناجح تُفرَّغ محلياً وفي DB.

---

## 10. الطلبات والمخزون — المرحلة 6 (القلب الحالي)

### 10.1 المشكلة التي أُغلقت
قبل المرحلة 6 كان `POST /api/orders` يضع الحالة `placed` **ويخصم المخزون فوراً بلا دفع**. أي شخص يستطيع تفريغ المستودع. هذا كان CRITICAL في التدقيق (6.8/10).

### 10.2 دورة الحياة الجديدة

```
Checkout
   ↓
pending_payment     ← حجز نشط، المخزون الفيزيائي كما هو
   ↓
  (Stripe لاحقاً)
   ↓
paid                ← هنا فقط يُخصم Product.stock
   ↓
dispatched

فشل / مهلة:
pending_payment → expired | cancelled
الحجز يُحرَّر، المتاح يعود
```

طلبات `placed` التاريخية بقيت `placed`. لا ندّعي أنها دُفعت. المخزون فيها كان قد خُصم سابقاً.

### 10.3 الحجز

جدول `InventoryReservation`:

- `active` · `released` · `consumed` · `expired`
- TTL من الإعداد `ORDER_RESERVATION_MINUTES` (افتراضي 15)
- `expiresAt` يُرجع من السيرفر وهو المرجع، لا المتصفح

**المتاح = Product.stock − مجموع الحجوزات النشطة غير المنتهية.**

كتالوج الـ API يعرض `stock` بمعنى **المتاح** حتى لا يظهر آخر قطعة محجوزة وكأنها حرّة.

### 10.4 ماذا يحدث في checkout

داخل معاملة واحدة:

1. قفل سلة المستخدم/الضيف.
2. قفل صفوف المنتجات بترتيب ثابت (`ORDER BY id`) لمنع deadlock.
3. إطلاق الحجوزات المنتهية على هذه المنتجات.
4. التحقق من المتاح.
5. إنشاء الطلب `pending_payment`.
6. تصوير السعر/الاسم/الصورة/التشطيب/الكمية في `OrderItem` — لا يتغير لاحقاً لو تغيّر سعر الكتالوج.
7. إنشاء حجز `active` لكل منتج.
8. **عدم** إنقاص `Product.stock`.
9. تفريغ السلة.
10. إرجاع:

```json
{
  "orderId": "NX-1850",
  "status": "pending_payment",
  "expiresAt": "2026-08-30T15:55:52.630Z",
  "subtotal": 240,
  "shipping": 0,
  "total": 240
}
```

إن نقص أي سطر تُلغى المعاملة كلها. لا حجز جزئي.

### 10.5 تأكيد الدفع — داخلي فقط

`confirmPaidOrder(orderId)`:

- يقفل المنتجات ثم الطلب.
- إن كان `paid` مسبقاً يعيد النجاح (idempotent — لـ webhook يعيد المحاولة).
- إن انتهت الحجوزات → 409، لا خصم.
- وإلا: خصم المخزون، الحجوزات `consumed`، الطلب `paid`.

`cancelOrder`:

- `pending_payment` → `cancelled` وتحرير الحجوزات (المخزون لم يُخصم أصلاً فلا يُعاد).
- `paid` / `dispatched` → `cancelRequestedAt` فقط، بلا استرجاع مال. الاسترجاع مرحلة لاحقة.

لا يوجد `POST /api/orders/:id/pay` عام.

### 10.6 عامل الانتهاء
كل دقيقة + مرة عند إقلاع السيرفر. إن أُعيد تشغيل السيرفر تُحرَّر الحجوزات المنتهية دون اعتماد على فتح المتصفح.

---

## 11. التأمين — المرحلة 5

نُفِّذت بعد تدقيق صُنِّف 6.8/10. الواجهة بقيت مقفلة.

| البند | التنفيذ |
|---|---|
| Clickjacking | إنتاج: `X-Frame-Options: SAMEORIGIN` + `frame-ancestors 'self'`. تطوير: بلا XFO و`frame-ancestors *` حتى يعمل iframe المعاينة |
| CSP | `script-src 'self'` بدون unsafe-inline/eval للسكربت. `style-src 'self' 'unsafe-inline'`. `object-src 'none'` |
| Cache | `Cache-Control: no-store, private` على `/api/me` و`/cart` و`/orders` |
| XSS في SPA | دالة `h()` على كل `innerHTML`. صور `images/…` فقط. حذف `onerror=` الإنلاين |
| JSON | حد 32kb. جسم تالف → `{ error: "Invalid JSON" }`. أكبر من الحد → 413 |
| 404 للـ API | `{ error: "Not found" }` حتى مع `..` في المسار |
| URI | أطول من 2048 → 414 |
| Origin | POST/PATCH/DELETE من أصل غريب → 403 |
| حدود | Auth 20/15د · API 120/د · Checkout 8/15د |
| ترقيم | `page` 1–200 · `perPage` 1–100 |
| بحث LIKE | `%` `_` `\` تُزال. استعلام رموز فقط → صفر نتائج لا الكتالوج كله |
| أخطاء 500 | دائماً `Internal server error` بلا Prisma/stack |
| Health | بلا تسريب `NODE_ENV` |
| X-Powered-By | معطّل |
| كوكي السلة | HMAC في القاعدة، الخام في الكوكي فقط |

ما بقي بعد المرحلة 5 وأُغلق في 6: خصم المخزون بلا دفع.

ما بقي مفتوحاً اليوم: يمكن لشخص إنشاء حجوزات `pending_payment` (حبس مؤقت 15 دقيقة، لا إفراغ دائم). المقيّد: 8 محاولات / 15 دقيقة لكل IP.

---

## 12. قاعدة البيانات — الترحيلات

| الترحيل | الغرض |
|---|---|
| `20260830142304_init` | المستخدم، المنتج، السلة، الطلب، المقالات، الـ drop |
| `20260830144327_sessions` | جلسات مُجزأة |
| `20260830145309_orders_seq` / `…_order_number_seq` | تسلسل `NX-1842+` |
| `20260830163000_inventory_reservations` | الحجوزات + حقول حالة الطلب |

لا `migrate reset`. البيانات القديمة محفوظة. الطلبات `placed` لم تُحوَّل إلى `paid`.

---

## 13. نتائج الاختبار (المرحلة 6)

42/42 نجحت، منها:

1. **آخر قطعة، ضيفان متزامنان:** واحد `201 pending_payment`، الآخر `409`. `Product.stock` بقي 1. حجز نشط واحد.
2. **انتهاء الحجز:** الحالة `expired`، المتاح عاد، ضيف جديد حجز بنجاح. `confirmPaidOrder` على المنتهي → 409.
3. **منتجان في طلب واحد:** حجزان في معاملة واحدة. إن فشل أحدهما لا طلب ولا حجز جزئي. المخزون الفيزيائي لم ينقص.
4. **تأكيد الدفع مرتين:** الخصم مرة واحدة. الثانية `alreadyPaid: true`.
5. **لقطة السعر:** `unitPrice = 111.11` بعدما صار سعر الكتالوج `999.99`.
6. **رجعة حساب/سلة:** إضافة ضيف، تسجيل، دمج، `no-store`، خروج، سلة ضيف فارغة.
7. **رجعة أمن:** Origin 403، JSON تالف 400، 404 JSON، CSP، health، سقف الصفحة، `qty: null`.

لا مسار دفع عام (`/api/orders/:id/pay` → 404).

---

## 14. ملفات الواجهة (الحالية)

| ملف | دور | قفل |
|---|---|---|
| `index.html` | الهيكل | لا إعادة تصميم |
| `styles.css?v=15` | الشكل | **مقفول** |
| `app.js?v=18` | المنطق | حد أدنى فقط |
| `api.js?v=2` | العميل | — |
| `catalog.js?v=3` | احتياطي | — |
| `images/` | تصوير الاستوديو | — |
| `favicon.svg` | الشعار | — |

`app.js` يفهم `orderId` من الرد. الحالة `pending_payment` تظهر في الداشبورد كما هي. لم يُغيَّر شكل لوحة النجاح.

---

## 15. ماذا لم يُنفَّذ (عن قصد)

هذه ليست نواقص نسيان، بل حدود المراحل:

1. **Stripe / PaymentIntent / webhooks / حقول بطاقة** — المرحلة 7 حصراً.
2. **لوحة Admin** لتغيير الحالة أو المخزون.
3. **إيميلات** تأكيد أو تسويق.
4. **مزامنة المفضلة** مع الحساب.
5. **مراجعات مستخدمين حقيقية** (الموجود نص دار ثابت).
6. **إلغاء طلب من الواجهة** للعميل.
7. **استرجاع أموال** بعد `paid`.
8. **إكمال كل صور الـ SKUs الناقصة** (معروف من المراحل الأولى).
9. إعادة تصميم الواجهة أو فك قفل `styles.css`.

---

## 16. المخاطر المتبقية وتقييم الإطلاق

| المخاطر | الشدة | ملاحظة |
|---|---|---|
| إنشاء حجوزات بدون دفع (حبس 15 د) | متوسطة | مقيّد بـ 8/15د لكل IP. بعد المهلة يتحرر |
| لا دفع حقيقي | — | متعمّد حتى المرحلة 7 |
| لا Admin | — | متعمّد |
| بعض الصور الناقصة | منخفضة | سقوط هادئ للصورة الاحتياطية |
| checkout ما زال يقول «simulated» | منخفضة | صادق إلى أن يصل Stripe |

**هل يصلح للعرض / الديمو؟ نعم.**  
**هل يصلح لمخزون حي ومال حقيقي؟ لا — حتى يُربط Stripe ويستدعي `confirmPaidOrder` من webhook.**

---

## 17. جاهزية المرحلة 7 (Stripe) — دون تنفيذها

الحدود الداخلية جاهزة:

- `confirmPaidOrder(orderId)` idempotent.
- `cancelOrder(orderId)` يفرّق بين ما قبل الدفع وما بعده.
- الحقول `paymentProvider` و `paymentReference` موجودة وNULL.
- الرد من checkout ليس `paid` أبداً.
- الكوكيز وCSP وOrigin كما هي؛ بطاقة Stripe يجب ألا تكسر CSP (`script-src 'self'` قد يحتاج ضبطاً مدروساً لنطاق Stripe فقط).

المطلوب لاحقاً (ولم يُبنَ): PaymentIntent، webhook يوقّع ويُتحقق منه، استدعاء `confirmPaidOrder`، واجهة دفع بلا إعادة تصميم الدار.

---

## 18. كيف يعمل التشغيل الآن

السيرفر يستمع على `0.0.0.0:3000` ويقدّم الـ SPA والـ API معاً.

```
PostgreSQL  →  user/db: nexora
API         →  node src/index.js
المعاينة    →  منفذ 3000
الصحة       →  GET /api/health  { status: "ok", database: "connected" }
```

في التطوير يُسمح بـ iframe. في الإنتاج: `SAMEORIGIN` و`frame-ancestors 'self'`.

---

## 19. الجدول الزمني المنطقي للعمل

1. **الهوية والفرونت.** دار داكنة سينمائية، كتالوج، فئات، PDP، سلة واجهة، مفضلة، بحث.
2. **إعادة التسمية إلى NEXORA** وتثبيت لغة الواجهة بعد رفض شرائح الفئات المتراكبة وتكرار الصور.
3. **قفل الواجهة** والانتقال للخلفية: Express، Prisma، جلسات، سلة رسمية، طلبات.
4. **تدقيق أمني (6.8/10)** — أخطر بند: خصم المخزون بلا دفع.
5. **المرحلة 5 — التأمين** بدون Stripe وبدون تغيير معمارية المخزون وقتها.
6. **المرحلة 6 — الحجوزات ودورة الطلب** — أغلقت بند المخزون الحرج.
7. **لم يبدأ:** Stripe، Admin.

---

## 20. الخلاصة في جملة

NEXORA اليوم دار إلكترونية كاملة الشكل والمنطق للمتصفح: كتالوج، سلة، حساب، طلب مرقّم، مخزون لا يُسرق بطلب وهمي، وجلسة محصّنة. ينقصها فقط جسر المال (Stripe) وغرفة الإدارة — وهما خارج نطاق ما أُنجز، وجاهزان للربط على الحدود التي تُركت لهما.

---

*نهاية التقرير. لا Stripe. لا Admin. الواجهة ما زالت مقفلة.*
