# PUBLIC frontend only. Copied into config.js at Vercel build.
# Leave empty for same-origin /api (recommended: Vercel rewrite /api → Railway).
# Not a secret. Never put Stripe or Supabase keys here.
NEXORA_API_BASE=
